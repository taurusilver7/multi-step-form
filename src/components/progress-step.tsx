
import type { Step } from "../types";

const ProgressSteps = ({
	currentStep,
	steps,
}: {
	currentStep: number;
	steps: Step[];
}) => {
	return <div>Progress Steps</div>;
};

export default ProgressSteps;
